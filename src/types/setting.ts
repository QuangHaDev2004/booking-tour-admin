export type WebsiteInfoItem = {
  websiteName: string;
  phone: string;
  email: string;
  address: string;
  facebook: string;
  zalo: string;
  logo: string;
  favicon: string;
};

export type RoleItem = {
  id: string;
  name: string;
  description: string;
  permissions: string[];
};
